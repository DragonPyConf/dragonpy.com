# Welcome to Jekyll!
#

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: DragonPy - A Python conference
description: >- # this means to ignore newlines until "baseurl:"
  A Python conference in Ljubljana, Slovenia, taking place on April 18 & 19, 2020.
  Followed by three days of sprints. Save the date!
url: "https://dragonpy.com" # the base hostname & protocol for your site, e.g. http://example.com
twitter_name: 'dragonpyconf'

# Build settings
markdown: kramdown
plugins:

sass:
    sass_dir: _sass

# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
